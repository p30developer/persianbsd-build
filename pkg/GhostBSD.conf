FreeBSD: {
    url: "pkg+https://pkg.FreeBSD.org/${ABI}/latest",
    mirror_type: "srv",
    enabled: yes
}

FreeBSD_base: {
    url: "pkg+https://pkg.FreeBSD.org/${ABI}/base",
    mirror_type: "srv",
    enabled: yes
}
